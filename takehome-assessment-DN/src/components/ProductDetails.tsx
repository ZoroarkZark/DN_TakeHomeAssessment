import type { imageURL } from "./ProductCard"

type ProductDetailsViewProps = {
    name: string,
    image: imageURL,
    description: string,
    price: number
}

const ProductDetailsView = () => {
    return (
        
    )
}

